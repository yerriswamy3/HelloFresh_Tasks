How to run Task 1 :
===================

To run the project, navigate to the folder and execute the command
           -- mvn clean install

To set the browser from command line use (Default browser is chrome)
           -- mvn clean install -Dbrowser=firefox 

To view reports:
---------------
           -- Open Cucumber-html-reports folder in the project root directory after execution and open file "overview-features.html"